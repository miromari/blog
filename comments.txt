У меня вопрос - насколько плохо, если переменные в PHP непроинициализированы?
В моем ДЗ это касается функции validate. Если проиницилазировать $error['title'] и $error['content'], то через empty($error) уже не проверить.  Как лучше?

Если инициализировать, то правильно сделать так?

Инициализация

    $error = ['title' => '','content' => ''];


Проверка
    if ($error['title'] == '' &&  $error['content'] == ''){}

